import 'package:flutter/material.dart';

class ParseSocialLogin extends StatelessWidget {
  const ParseSocialLogin({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
